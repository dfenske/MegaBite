@model List<String>

@{
    var stringToDisplay = "";
    int count = 1;
    foreach (string s in Model)
    {
        if (count == Model.Count)
        {
            stringToDisplay += s;
        }
        else {
            stringToDisplay += (s + ", ");
        }
        count++;
    }

}

@stringToDisplay